# ── Admin scripts only (scripts/seed.mjs, scripts/set-claims.mjs) ──
# Path to a service account JSON (not needed when targeting emulators)
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
# For emulator seeding:
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
# FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
